// 関数の作り方
const squarecalc = (number) => {
    console.log(Math.pow(number , 2));
  }
  
  // 関数の呼び出し方
  let number = Math.floor(Math.random() * 10) + 1;
  console.log(number);
  squarecalc(number);
  